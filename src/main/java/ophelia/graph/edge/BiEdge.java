package ophelia.graph.edge;

import ophelia.collections.pair.Pair;

/**
 * A standard edge that connects two nodes.
 * @author Steven Weston
 */
public interface BiEdge<N, P extends Pair<N>> extends Edge<N, P> {
}
